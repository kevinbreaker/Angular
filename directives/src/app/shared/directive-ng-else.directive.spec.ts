import { DirectiveNgElseDirective } from './directive-ng-else.directive';

describe('DirectiveNgElseDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveNgElseDirective();
    expect(directive).toBeTruthy();
  });
});
