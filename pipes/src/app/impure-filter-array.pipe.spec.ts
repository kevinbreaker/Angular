import { ImpureFilterArrayPipe } from './impure-filter-array.pipe';

describe('ImpureFilterArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ImpureFilterArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
